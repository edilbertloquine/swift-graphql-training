query CharacterList($page: Int) {
  characters(page: $page) {
    info {
      count
      pages
      prev
      next
    }
    results {
      id
      name
      status
      species
      image
    }
  }
}
